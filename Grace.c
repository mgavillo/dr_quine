#include <stdio.h>
#define file "Grace_kid.c"
#define str "#include <stdio.h>%c#define file %cGrace_kid.c%c%c#define str %c%s%c%c#define fun main(fprintf(fopen(file, %cw%c), str, 10, 34, 34, 10, 34, str, 34, 10, 10 );}%cfun()"
#define fun() main(){fprintf(fopen(file, "w"), str, 10, 34, 34, 10, 34, str, 34, 10, 34, 34, 10);}
fun();